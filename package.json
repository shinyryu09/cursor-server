{
  "name": "cursor-server",
  "version": "1.0.0",
  "description": "Node.js server for Xcode Code Intelligence integration with Cursor CLI",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"No tests specified\" && exit 0",
    "lint": "echo \"No linting configured\" && exit 0",
    "format:check": "echo \"No formatting configured\" && exit 0",
    "build": "echo \"No build process needed\" && exit 0"
  },
  "keywords": ["cursor", "xcode", "code-intelligence", "nodejs", "express"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "body-parser": "^1.20.2",
    "axios": "^1.6.0",
    "child_process": "^1.0.2",
    "path": "^0.12.7",
    "fs": "^0.0.1-security"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
